import presentationBuilderConfig from './presentationBuilder.config.ts';

export default angular.module('structures_site.componants.views.presentationBuilder', [])

.config(presentationBuilderConfig).name;

import * as UIRouter from 'angular-ui-router';

function pdbSearchConfig($stateProvider: UIRouter.IStateProvider) : void {
	
}

pdbSearchConfig.$inject = [
	'$stateProvider'
];

export default pdbSearchConfig;

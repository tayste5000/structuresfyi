import * as UIRouter from 'angular-ui-router';

function pvLinkConfig($stateProvider: UIRouter.IStateProvider) : void {
	
}

pvLinkConfig.$inject = [
	'$stateProvider'
];

export default pvLinkConfig;
